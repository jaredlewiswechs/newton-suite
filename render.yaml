# render.yaml
# Newton API - Render Deployment Configuration

services:
  - type: web
    name: newton-kernel
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python -m uvicorn newton_os_server:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHONPATH
        value: /opt/render/project/src
      - key: PYTHONUNBUFFERED
        value: "1"
      - key: NEWTON_STORAGE
        value: /tmp/newton
    healthCheckPath: /health
    autoDeploy: true
