blueprint ParameterTest
  starts result at 0

when add_numbers(x, y)
  calc x plus y as sum
  set result to sum
finfr "Addition complete"

when multiply(a, b, c)
  calc a times b as temp
  calc temp times c as product
  set result to product
finfr "Multiplication complete"
