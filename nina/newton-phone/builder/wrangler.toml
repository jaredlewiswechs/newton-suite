# Cloudflare Pages configuration for Newton Interface Builder

name = "newton-interface-builder"
compatibility_date = "2024-01-01"

# For Cloudflare Pages, deploy the interface-builder directory
# Run: npx wrangler pages deploy interface-builder --project-name=newton-interface-builder

[site]
bucket = "./"

# Environment variables
[vars]
ENVIRONMENT = "production"
