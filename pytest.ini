[pytest]
addopts = --maxfail=1

# Ignore a few script-like integration tests that are intended as standalone
# runners rather than pytest units. This keeps the automated suite fast.
; --ignore paths
; Adjust or remove ignores when you want a full integration run.
python_files = test_*.py
markers =
    integration: marks tests that require external services
